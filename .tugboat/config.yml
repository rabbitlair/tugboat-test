services:
    php:
        image: tugboatqa/php:apache
        http: true
        https: true
        commands:
            init:
                - ln -snf "${TUGBOAT_ROOT}" "${DOCROOT}"

        urls:
            alias0:
              - url: /
                visualdiff:
                    threshold:
                        desktop: 90
                        mobile: 95
                        tablet: 99
