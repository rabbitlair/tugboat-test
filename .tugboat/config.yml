services:
    apache:
        image: tugboatqa/httpd
        aliases: test
        http: true
        https: false
        commands:
            init: ln -snf "${TUGBOAT_ROOT}" "${DOCROOT}"
        visualdiffs:
            - url: /
              # aliases: [ :default, foo, bar, baz ]
            - /foo
            - /bar
            - /baz
